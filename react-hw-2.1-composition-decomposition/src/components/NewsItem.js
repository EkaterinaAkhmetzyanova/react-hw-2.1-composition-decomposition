/** Виджет для создания одной новости из списка, принимает логотип, содержание новости и ссылку на новость */
export default function NewsItem({logo, title, link}) {
    return (
        <li></li>
    )
}